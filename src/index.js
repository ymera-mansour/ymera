const { CloudAgentDelegator } = require('./delegator');
const { CloudAgent } = require('./agent');
const { RemoteCloudAgent } = require('./remote-agent');

module.exports = {
    CloudAgentDelegator,
    CloudAgent,
    RemoteCloudAgent
};
